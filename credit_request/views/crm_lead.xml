<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="crm_lead_inherit_credit_request" model="ir.ui.view">
    <field name="name">crm.lead.inherit.credit.request</field>
    <field name="model">crm.lead</field>
    <field name="inherit_id" ref="crm.crm_lead_view_form"/>
    <field name="arch" type="xml">

        <xpath expr="//field[@name='stage_id']" position="before">
            <field name="request_stage" invisible="1"/>
            <button name="new_request" string="Solicitud de crédito" type="object" class="oe_highlight" attrs="{'invisible': [('request_stage', '=', False)]}"/>
        </xpath>

        <xpath expr="//button[@name='action_schedule_meeting']" position="before">
          <button type="object" name="action_view_task_request" class="oe_stat_button" icon="fa-tasks" attrs="{'invisible': [('request_task_count', '=', 0)]}">
            <field string="Solicitudes de crédito" name="request_task_count" widget="statinfo"/>
          </button>
        </xpath>

    </field>
  </record>

</odoo>

